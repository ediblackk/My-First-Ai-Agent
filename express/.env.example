# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/make-a-wish

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Solana Configuration
SOLANA_NETWORK=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_PRIZE_POOL_WALLET=your-prize-pool-wallet-address-here
SOLANA_ADMIN_WALLET=your-admin-wallet-address-here

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# AI Service Configuration
AI_SERVICE_API_KEY=your-ai-service-api-key-here

# Token Configuration
DEFAULT_SOL_TO_CREDITS_RATE=1  # Fallback rate if price feed fails
PRIZE_POOL_PERCENTAGE=80
ADMIN_PERCENTAGE=20

# Note: For production:
# 1. Generate a new JWT secret
# 2. Never commit real wallet addresses or API keys
# 3. The SOL to Credits rate is dynamically calculated based on token value
#    - Uses CoinGecko API to get current SOL price
#    - Adjusts rate to maintain consistent credit value in USD
#    - DEFAULT_SOL_TO_CREDITS_RATE is only used as fallback
# 4. You can create a Solana wallet using:
#    - Phantom wallet browser extension
#    - Solana CLI: solana-keygen new --outfile prize-pool-wallet.json
#    - Solflare wallet
